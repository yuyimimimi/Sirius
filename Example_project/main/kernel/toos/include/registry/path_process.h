#ifndef __PATH_PROCESS_H__
#define __PATH_PROCESS_H__
/**
 * @file path_process.h
 * @brief This file contains the function prototypes for path processing.
 * if the path is valid, it will try to create the directory if it doesn't exist.
*/
int check_path(char *path);


#endif